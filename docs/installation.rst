============
Installation
============

At the command line::

    $ easy_install django-entropy

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-entropy
    $ pip install django-entropy
